//
//  {{ module_info.name }}Interactor.swift
//  {{ developer.company }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import RxSwift

struct {{ module_info.name }}Interactor: {{ module_info.name }}ViewOutput {
	private let disposeBag: DisposeBag
	private let requestSubject: PublishSubject<{{ module_info.name }}Flow.Request>
	private let output: {{ module_info.name }}InteractorOutput

	// MARK: - Initializer

	init(output: {{ module_info.name }}InteractorOutput) {
		disposeBag = DisposeBag()
		requestSubject = PublishSubject()

		self.output = output

		bindRequestEvent()
	}

	// MARK: - {{ module_info.name }}ViewOutput

	var handleRequest: AnyObserver<{{ module_info.name }}Flow.Request> {
		return requestSubject.asObserver()
	}

	// MARK: - Private

	private func bindRequestEvent() {
		/*
			requestSubject
				.asObservable()
				.map { request in
					switch request {
					case .myRequest: return self.handleMyRequest()
					}
				}
				.bind(to: output.handleResponse)
				.disposed(by: disposeBag)
		*/
	}
}
