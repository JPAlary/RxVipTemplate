//
//  {{ module_info.name }}Interactor.swift
//  {{ developer.company }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import RxSwift
import RxCocoa

struct {{ module_info.name }}Presenter: {{ module_info.name }}ViewInput {
	private let disposeBag: DisposeBag
	private let input: {{ module_info.name }}PresenterInput

	// MARK: - Initializer

	init(input: {{ module_info.name }}PresenterInput) {
		disposeBag = DisposeBag()

		self.input = input
	}

	// MARK: {{ module_info.name }}ViewInput

	var viewModel: Driver<{{ module_info.name }}Flow.ViewModel> {
		/*
			return input
				.response
				.map { response in
					switch response {
					case .myResponseCase:
						return {{ module_info.name }}Flow.ViewModel(...)
					}
				}
		*/

		return .never()
	}
}
